<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="runningtracker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_athletics"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_api_key" />

        <activity android:name=".view.running.ResultActivity" />
        <activity android:name=".view.main.MainActivityHome" />
        <activity android:name=".view.running.MainActivity" />
        <activity android:name=".view.running.MainActivityOffline" />
        <activity android:name=".view.main.WeatherSuggestion"/>
        <activity android:name=".view.main.LoginActivity"/>
        <activity android:name=".view.suggest_place.suggest_place">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.main.RegisterActivity"></activity>
    </application>

</manifest>